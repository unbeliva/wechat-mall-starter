<view>
  <view>自提门店：
    <picker mode="selector" range="{{stores}}" bindchange="onStoreChange">
      <view>{{selectedStore.name || '请选择门店'}}</view>
    </picker>
  </view>
  <view style="margin-top:12px">
    <button bindtap="createOrder">确认下单（自提）</button>
  </view>
</view>
